{% set <FieldHandle> = entry.<FieldHandle>.all() %}

{% if <FieldHandle> is not empty %}

  <h3><FieldName></h3>

  <ul class="<FieldHandle>">
    {% for tag in <FieldHandle> %}
      <li><a href="{{ url('tags/'~tag.slug) }}">{{ tag.title }}</a></li>
    {% endfor %}
  </ul>
  
{% endif %}
